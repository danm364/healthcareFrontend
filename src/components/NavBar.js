import React from 'react';
import { Link } from 'react-router-dom';
import './NavigationBar.css'; // Ensure to create a CSS file for styling

const NavigationBar = () => {
    return (

    );
};

export default NavigationBar;